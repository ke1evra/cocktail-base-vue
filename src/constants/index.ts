export const FIELD_NAMES = Object.freeze({
  TOKEN: 'token',
});

export const BASE_COCKTAIL_CODES = Object.freeze(['margarita', 'mojito', 'a1', 'kir']);

export const DEFAULT_COCKTAIL_CODE = BASE_COCKTAIL_CODES[0];
